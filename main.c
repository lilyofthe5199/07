#include <stdio.h>
#include <stdlib.h>

/* run this program using the console pauser or add your own getch, system("pause") or input loop */

int main(void)
{
	int flag = 1;
	
	while(flag != 0)
	{
	int y; // y의 범위: while 블록 
	y = 3;
	flag = 0;
	}
	return 0;
}
